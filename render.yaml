services:
  - type: web
    name: mansurbek
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: FLASK_SECRET
        sync: false